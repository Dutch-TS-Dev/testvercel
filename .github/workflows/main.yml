name: ChatGPT CodeReviewer

on:
  push:
    branches:
      - main

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run ChatGPT Code Review
        uses: anc95/ChatGPT-CodeReview@v1.0.21
        with:
          openai_api_key: ${{ env.GITHUB_TOKEN }}
          language: javascript
          review_comment_lgtm: false
